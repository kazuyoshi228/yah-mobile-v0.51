# ロードマップ v0.6 → v1.0（一般公開）

作成: 2026-07-08 ／ 現在地: **v0.6**（eSIMAccess 本番稼働＋大規模リファクタ完了）

## 現在地（v0.6 = 済み）
- ✅ eSIMAccess **単一プロバイダで本番稼働中**（発行/同期/topup/cancel・販売停止ガード・死活監視・多層防御Webhook・自動返金・§8 cancel連携）
- ✅ 返金機能（Lane A/B・キルスイッチ・5言語メール）／可観測性（Error Reporting＋フロント収集＋S9到達＋S10死活）
- ✅ 大規模リファクタ P0–P5（dev。**本番未反映**）／運用ランブック
- ⚠️ **未検証**：実発注の通し（実購入→発行→QR→返金）は一度も実施していない
- ⚠️ **招待制ゲートON**：`allowed_emails` に無いユーザーは購入不可（＝まだ一般公開されていない）

---

## v0.7 — 「動くことを証明する」（稼働中プロダクトの安全確認）
> 目的：本番で売っているのに未検証、が最大リスク。まず実際に通す＋リファクタを本番へ。

| # | 項目 | 内容・指示 | 担当 | 規模 |
|---|---|---|---|---|
| 0.7-1 | **実発注 E2E 検証**（plan_v0.51_v2 §3 受け入れ基準のフル範囲） | 招待済み自アカウントで **1GB/¥980 を実購入**し、以下を通す：①**発行**（数十秒でesim発行）②**QR/インストール**表示・期限 ③`esim_links` に `provider:"esimaccess"`/`iccid`/`lpaProfile`/`providerRef` ④**同期**（`onEsimSyncRequested`＝MyPage更新時に権威データ取得）⑤**残量アラート**（`DATA_USAGE`/`VALIDITY_USAGE` webhook→通知）⑥**topup**（※データ枯渇が要るため実検証は後回し可＝別途 or 擬似）⑦**cancel→Stripe返金→返金メール**（/admin返金） | **あなた購入**／私がデータ検証 | 小〜中 |
| 0.7-2 | **topupリトライ不具合 修正** | `webhooks.ts` の `handleProvisioningFailure` で topup時 `esimLinkUuid` が常にundefined → リトライが必ず失敗する既存バグ（チップ task_14ec50b7）。esim_link解決を修正＋テスト | 私 | 小 |
| 0.7-3 | **リファクタを本番反映** | P0–P5（挙動不変）を functions＋hosting へ。※`incidentRunRetryNow`/`adminMigrateIsActiveToBoolean` の削除確認プロンプトが出る（正常） | あなた（reauth＋deploy指示）／私が実行 | 小 |
| 0.7-4 | **実トラフィック下の監視確認** | `provider_health.esimaccess`（残高/status）・低残高アラート・返金webhook・S9通知が実データで動くか目視 | 私＋あなた | 小 |

**ゲート**：実発注が1件、発行→返金まで完走すること。

---

## v0.8 — 「表記と法務を正す」（一般公開の前提）
> 目的：「24/7チャット」等の未提供機能を謳ったまま公開＝景表法リスク。ここを閉じないと一般公開できない。

| # | 項目 | 内容・指示 | 担当 | 規模 |
|---|---|---|---|---|
| 0.8-1 | **サポート/チャット整合（SUP）** | (a)AIチャット(別PJ)を稼働、または **(b)フォーム主導へ表記修正**（"24/7 chat"の暫定化・CTAを`/contact`へ・Terms/Privacy/Cookieの連絡先有効化）。どちらか決める | あなた=方針決定／私=実装 | 小〜中 |
| 0.8-2 | **法務整合（特商法/規約）** | 特商法表記・利用規約・プライバシーを **eSIMAccessモデル＋返金ポリシー**に整合。問い合わせ窓口が実際に届くか確認 | あなた（内容確定）／私=文言反映 | 中 |
| 0.8-3 | **返金ポリシーの一貫性** | 購入前チェックボックス・FAQ・規約・実装(自動/手動返金)の記述を突き合わせ矛盾解消 | 私 | 小 |

**ゲート**：サイト上の約束（サポート・返金・事業者情報）が実装と一致すること。

---

## v0.9 — 「固める」（GA前ハードニング）
> 目的：公開後に効いてくる守り。挙動不変中心。

| # | 項目 | 内容・指示 | 担当 | 規模 |
|---|---|---|---|---|
| 0.9-1 | **S3 アクセシビリティ** | 購入ドロワー・問い合わせフォーム優先で aria/キーボード/フォーカス点検 | 私 | 中 |
| 0.9-2 | **firestore.rules: plans新フィールド検証** | provider/providerPlanId/wholesalePriceUsd 等の最小バリデーション追加（要承認・rules変更） | あなた承認／私実装 | 小 |
| 0.9-3 | **柱1 bappyWebhook 認証の結論** | 休眠だが `// TODO: Verify Bappy signature` が残存。署名なし受信を(a)IP許可等で固める or (b)休眠として受容を明記 | 私 | 小 |
| 0.9-4 | **ドキュメント最新化** | `api_functions.md`/`firestore_schema.md` を柱2後の実装に整合（provider系フィールド反映） | 私 | 小 |

**ゲート**：a11y基本OK・rules整合・ドキュメントが実装と一致。

---

## v1.0 — 「扉を開ける」（一般公開 GA）
> 目的：招待制を外し、一般ユーザーに開放。最終QAと運用体制。

| # | 項目 | 内容・指示 | 担当 | 規模 |
|---|---|---|---|---|
| 1.0-1 | **招待制ゲート解除** | `requireAuth` の `isEmailAllowed` ゲートを一般開放に切替（フラグ化 or 撤去）。※要承認・段階公開も選択可 | あなた=Go判断／私=実装 | 小 |
| 1.0-2 | **公開前 最終QA** | 主要導線を全言語で通し確認（購入/ログイン/MyPage/topup/返金/問い合わせ）＋モバイル実機 | 私＋あなた | 中 |
| 1.0-3 | **集客・SEO・計測の点火** | 比較表の価格整合(済)・OGP・sitemap・robots・Analyticsファネル・reCAPTCHA許可ドメイン最終確認 | 私 | 小 |
| 1.0-4 | **運用体制の確認** | ランブック最終版・アラート到達・返金/障害の手順を再確認。残高チャージ運用の確立 | あなた | 小 |

**GAゲート**：招待制解除・全言語QA通過・計測稼働・運用手順確定。

---

## 任意（GA後でよい・バックログ）
| 項目 | 内容 |
|---|---|
| S5 lastSignedIn | 毎ログイン更新（現状ほぼ未更新） |
| S8 依存自動更新 | Dependabot/Renovate |
| S6' admin `any`削減 | 型安全性の底上げ |
| mailer/webhooks 追加リファクタ | v0.6で意図的に見送った項目（挙動不変だが決済経路のため慎重に） |

## 依存・順序メモ
- **v0.7-1（実発注）は最優先**：本番で未検証のまま売っている状態を解消する。
- v0.8（表記/法務）を閉じるまでは**一般公開しない**（招待制のまま運用は可）。
- リファクタ本番反映(0.7-3)は挙動不変なので、v0.8のSUP実装と同じデプロイに相乗り可。
- v1.0-1（招待解除）は他が全部揃ってから最後に。
